[defaults]
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
# If the collection is not installed system-wide, drop it under ./collections:
#   ansible-galaxy collection install hitachivantara.vspone_block -p ./collections
collections_paths = ./collections:~/.ansible/collections:/usr/share/ansible/collections
