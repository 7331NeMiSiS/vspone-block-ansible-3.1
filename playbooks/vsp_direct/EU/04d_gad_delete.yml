---
# ###################################################################
# GAD lifecycle 4/4 : Delete the pair (keeps the S-VOL)
# Run: ansible-playbook 04d_gad_delete.yml -J
# should_delete_svol: false leaves the DC2 volume in place.
# (Full teardown of pair + both volumes is 05_cleanup.yml.)
# ###################################################################
- name: Delete GAD pair
  hosts: localhost
  gather_facts: false

  vars_files:
    - vault/storage_vars.yml
    - vars/demo_vars.yml

  vars:
    connection_info:
      address: "{{ storage_address }}"
      username: "{{ vault_storage_username }}"
      password: "{{ vault_storage_secret }}"
    secondary_connection_info:
      address: "{{ secondary_storage_address }}"
      username: "{{ vault_secondary_storage_username }}"
      password: "{{ vault_secondary_storage_secret }}"

  tasks:
    - name: Delete the GAD pair
      hitachivantara.vspone_block.vsp.hv_gad:
        connection_info: "{{ connection_info }}"
        secondary_connection_info: "{{ secondary_connection_info }}"
        state: absent
        spec:
          copy_group_name: "{{ copy_group_name }}"
          primary_volume_id: "{{ primary_volume_id }}"
          should_delete_svol: false
      register: delete_result

    - name: Show delete result
      ansible.builtin.debug:
        var: delete_result
