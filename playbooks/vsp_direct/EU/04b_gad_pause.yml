---
# ###################################################################
# GAD lifecycle 2/4 : Pause (split) the pair
# Run: ansible-playbook 04b_gad_pause.yml -J
# After this, the S-VOL stops mirroring - verify PSUS/split from host.
# Resume later with:  state: resync  (one-line change if you need it).
# ###################################################################
- name: Pause (split) GAD pair
  hosts: localhost
  gather_facts: false

  vars_files:
    - vault/storage_vars.yml
    - vars/demo_vars.yml

  vars:
    connection_info:
      address: "{{ storage_address }}"
      username: "{{ vault_storage_username }}"
      password: "{{ vault_storage_secret }}"
    secondary_connection_info:
      address: "{{ secondary_storage_address }}"
      username: "{{ vault_secondary_storage_username }}"
      password: "{{ vault_secondary_storage_secret }}"

  tasks:
    - name: Pause (split) the GAD pair
      hitachivantara.vspone_block.vsp.hv_gad:
        connection_info: "{{ connection_info }}"
        secondary_connection_info: "{{ secondary_connection_info }}"
        state: split
        spec:
          copy_group_name: "{{ copy_group_name }}"
          copy_pair_name: "{{ copy_pair_name }}"
      register: pause_result

    - name: Show pause result
      ansible.builtin.debug:
        var: pause_result
